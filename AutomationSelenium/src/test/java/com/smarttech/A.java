package com.smarttech;

public interface A extends B{
	// interface = class(special type)
	// why ?= to handle multiple inheritance

	abstract void getsalary();


	//non abstract class = non abstract method
	//abstract class = non abstract + abstract method
		
	// interface = only abstract method
	
}
